name: Build and deploy backend to Azure

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: npm
          cache-dependency-path: Backend/package-lock.json

      - name: Instalar dependencias
        working-directory: Backend
        run: npm ci

      - name: Preparar backend
        run: |
          mkdir backend-deploy
          cp -r Backend/src backend-deploy/
          cp Backend/package.json backend-deploy/
          cp Backend/package-lock.json backend-deploy/

      - name: Subir artefacto
        uses: actions/upload-artifact@v4
        with:
          name: backend-app
          path: backend-deploy

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Descargar artefacto
        uses: actions/download-artifact@v4
        with:
          name: backend-app
          path: app

      - name: Publicar backend en Azure
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'NOMBRE-EXACTO-DEL-APP-SERVICE-BACKEND'
          slot-name: 'Production'
          package: app
          publish-profile: ${{ secrets.NOMBRE_DEL_SECRETO_CREADO_POR_AZURE }}
